// studentScore = ? 
// maxScore = 100 
// percent = studentScore/maxScore * 100 
//print to console.

let studentScore = 15
let maxScore = 17
let percent = (studentScore / maxScore) * 100 
console.log(`you got a ${percent} % on this test.`)